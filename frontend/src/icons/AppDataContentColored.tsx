import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/appDataContent.svg'

const AppDataContentColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default AppDataContentColored
