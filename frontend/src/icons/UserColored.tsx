import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/user.svg'

const UserColored = (props: any) => <Icon component={comSvg} {...props} />

export default UserColored
