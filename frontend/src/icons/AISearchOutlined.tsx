import Icon from '@ant-design/icons'
import { ReactComponent as analysisSvg } from './svg/outlined/aiSearch.svg'

const AISearchOutlined = (props: any) => (
    <Icon component={analysisSvg} {...props} />
)

export default AISearchOutlined
