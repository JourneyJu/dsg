import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/appModal.svg'

const AppModalColored = (props: any) => <Icon component={comSvg} {...props} />

export default AppModalColored
