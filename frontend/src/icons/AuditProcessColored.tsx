import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/auditProcess.svg'

const AuditProcessColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default AuditProcessColored
