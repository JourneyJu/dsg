import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/sceneAnalysis.svg'

const SceneAnalysisColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default SceneAnalysisColored
