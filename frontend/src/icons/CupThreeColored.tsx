import Icon from '@ant-design/icons'
import { ReactComponent as component } from './svg/colored/cupThree.svg'

const CupThreeColored = (props: any) => (
    <Icon component={component} {...props} />
)

export default CupThreeColored
