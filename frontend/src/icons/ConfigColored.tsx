import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/config.svg'

const ConfigColored = (props: any) => <Icon component={comSvg} {...props} />

export default ConfigColored
