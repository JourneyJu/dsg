import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/outlined/sceneAnalysis.svg'

const SceneAnalysisOutlined = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default SceneAnalysisOutlined
