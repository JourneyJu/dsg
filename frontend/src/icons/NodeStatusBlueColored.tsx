import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/nodeStatusBlue.svg'

const NodeStatusBlueColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default NodeStatusBlueColored
