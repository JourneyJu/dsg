import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/AI.svg'

const AIColored = (props: any) => <Icon component={comSvg} {...props} />

export default AIColored
