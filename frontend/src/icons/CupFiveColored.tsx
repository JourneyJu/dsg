import Icon from '@ant-design/icons'
import { ReactComponent as component } from './svg/colored/cupFive.svg'

const CupFiveColored = (props: any) => <Icon component={component} {...props} />

export default CupFiveColored
