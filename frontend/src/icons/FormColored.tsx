import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/form.svg'

const FormColored = (props: any) => <Icon component={comSvg} {...props} />

export default FormColored
