import Icon from '@ant-design/icons'
import { ReactComponent as addIconSvg } from './svg/outlined/description.svg'

const DescriptionOutlined = (props: any) => (
    <Icon component={addIconSvg} {...props} />
)

export default DescriptionOutlined
