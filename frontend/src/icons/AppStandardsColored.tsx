import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/appStandards.svg'

const AppStandardsColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default AppStandardsColored
