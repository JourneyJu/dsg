import Icon from '@ant-design/icons'
import { ReactComponent as titleSvg } from '@/assets/DataAssetsCatlg/icon1.svg'

const TitleIconColored = (props: any) => (
    <Icon component={titleSvg} {...props} />
)

export default TitleIconColored
