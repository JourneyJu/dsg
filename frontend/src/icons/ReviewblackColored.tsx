import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/reviewOutlined.svg'

const ReviewblackColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default ReviewblackColored
