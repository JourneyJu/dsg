import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/nodeStatusGray.svg'

const NodeStatusGrayColored = (props: any) => (
    <Icon component={comSvg} {...props} />
)

export default NodeStatusGrayColored
