import Icon from '@ant-design/icons'
import { ReactComponent as comSvg } from './svg/colored/domain.svg'

const DomainColored = (props: any) => <Icon component={comSvg} {...props} />

export default DomainColored
